name = "kardo-api-proxy"
main = "src/worker.js"
compatibility_date = "2026-06-04"
workers_dev = true
preview_urls = false

[vars]
PUBLIC_SITE_URL = "https://tools.songmatin.com"
ALLOWED_ORIGINS = "https://tools.songmatin.com,http://localhost:8000"

# If songmatin.com is managed by Cloudflare DNS, enable this route after testing.
# routes = [
#   { pattern = "tools.songmatin.com/api/*", zone_name = "songmatin.com" }
# ]
